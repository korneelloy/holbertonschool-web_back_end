#!/usr/bin/env python3
"""returning floor of float"""
import math


def floor(n: float) -> int:
    """ type-annotated function returning floor of float
    argv :  n: float
    Returns: int
    """
    return (math.floor(n))
